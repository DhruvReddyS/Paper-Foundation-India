import MillMasterGame from "@/components/games/MillMasterGame";

export const metadata = { title: "Paper Mill Shuffle | Paper Foundation India" };

export default function MillMasterPage() {
  return <MillMasterGame />;
}
