import GrowOrShredGame from "@/components/games/GrowOrShredGame";

export const metadata = { title: "Grow or Shred | Paper Foundation India" };

export default function GrowOrShredPage() {
  return <GrowOrShredGame />;
}
